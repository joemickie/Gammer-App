class GameManager {
  async createGame(gameName, rules, mechanics) {
    // Implementation for creating a new game
  }

  async addGameToRoom(roomId, gameId) {
    // Implementation for adding a game to a room
  }
}

module.exports = GameManager;
